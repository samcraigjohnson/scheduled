extends layout

block content
	h1= title
	#schools
	- each school in schools
		div.school
		div.created_at= school.created_at
		div.name
			a(href="/blog/"+school._id)!= school.name
		div.location = article.location_city + "," article.location_state

